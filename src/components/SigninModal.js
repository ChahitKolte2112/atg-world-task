import React from "react";
import LoginModal from "./LoginModal/LoginModal";
const SigninModal = () => {
    return (
        <div>
            <LoginModal />
        </div>
    );
};

export default SigninModal;
